# Findings reviewed once and pinned by fingerprint, so the same shape appearing anywhere new
# still fails the gate. A fingerprint is commit:path:rule:line — it does not generalise.
#
# **A history rewrite invalidates every line in this file.** The fingerprint names a commit, so
# rewriting one (git-filter-repo, a rebase, git-moonlighter moving author dates) gives it a new
# sha and every pin here stops matching. The gate then refuses a push with findings that were
# reviewed months ago. That is the gate working, not a false alarm: re-review, regenerate, commit.
# This file was last regenerated after exactly that, on 2026-08-26.
#
# Every entry below was inspected by hand and is one of:
#
#   * a Sparkle *public* EdDSA key, which is published on purpose (releasing.md, "Keys");
#   * a fixture in a test — a fake credential-helper response, a generated invitation token, a
#     key minted inside the test that uses it;
#   * a Swift type annotation on a key-shaped property name, which the generic rule reads as an
#     assignment even though the line binds a type rather than a value.
#
# None is a credential. Nothing here grants access to anything.
#
# Regenerate after review with:
#   gitleaks git --report-format json --report-path leaks.json --exit-code 0

# Two fingerprint shapes appear below, and both are needed. A scan that walks history from a
# ref records commit:path:rule:line; one that reaches a blob through a path whose file no
# longer exists records path:rule:line with no commit. The Skalman-era entries are the second
# kind: that tree was renamed to Threading, so those paths exist only in history.

1cb700c416f813c25b9b91baedfeff174a6a01c2:Tests/ThreadingTests/AppUpdaterTests.swift:generic-api-key:54
511fed1c50ff8a1879eda0968a45cceff6ae486a:docs/architecture/releasing.md:generic-api-key:206
594e357c331a7b79ed23f2372675cd6d24a5f285:Service/ThreadingControlPlane/test/service.test.ts:generic-api-key:55
6f153b24d18810a9774b80e6bac9481484d1a0c0:Tests/ThreadingTests/RemoteGuestInvitationTests.swift:generic-api-key:14
Sources/Skalman/Core/Remote/RemoteNotificationService.swift:generic-api-key:427
Tests/SkalmanTests/RemoteServerIntegrationTests.swift:generic-api-key:511
Tests/SkalmanTests/RemoteServerIntegrationTests.swift:generic-api-key:523
Tests/SkalmanTests/RemoteServerIntegrationTests.swift:generic-api-key:536
Tests/SkalmanTests/RemoteServerIntegrationTests.swift:generic-api-key:571
Tests/SkalmanTests/RemoteWebSocketTests.swift:generic-api-key:24
Tests/SkalmanTests/RemoteWebSocketTests.swift:generic-api-key:50
cc509f204580817f1640fc0b0600e7141cd73a3d:Tests/ThreadingTests/GitHubCredentialsTests.swift:generic-api-key:146
cc509f204580817f1640fc0b0600e7141cd73a3d:ThreadingRemoteKit/Tests/ThreadingRemoteKitTests/RemoteProtocolTests.swift:generic-api-key:743
cc509f204580817f1640fc0b0600e7141cd73a3d:ThreadingRemoteKit/Tests/ThreadingRemoteKitTests/RemoteProtocolTests.swift:generic-api-key:764
